# ArmedMusic Cloud Deployment ☁️

Разверните вашего бота в облаке для работы 24/7 независимо от вашего компьютера.

## 🚀 Быстрый старт

### 1. Railway (Самый простой)
```bash
# Подготовьте проект
cp env.example .env
# Заполните API_ID, API_HASH, BOT_TOKEN, OWNER_ID

# Создайте GitHub репозиторий
git init && git add . && git commit -m "Deploy"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main

# Разверните на Railway
# Перейдите: https://railway.app -> Connect GitHub -> Deploy
```

### 2. DigitalOcean VPS
```bash
# Создайте Droplet ($6/месяц)
# Подключитесь по SSH
ssh root@YOUR_IP

# Установите Docker
curl -fsSL https://get.docker.com | sh
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Разверните бота
git clone YOUR_REPO
cd ArmedMusic
cp env.example .env
# Заполните .env
docker-compose up -d
```

## 📋 Полные гайды

| Платформа | Сложность | Стоимость | Гайд |
|-----------|-----------|-----------|------|
| **Railway** | Очень просто | Бесплатно | [DEPLOY-RAILWAY.md](DEPLOY-RAILWAY.md) |
| **DigitalOcean** | Средне | $6/месяц | [DEPLOY-DIGITALOCEAN.md](DEPLOY-DIGITALOCEAN.md) |
| **Render** | Просто | Бесплатно | `render.yaml` |
| **Общий гайд** | - | - | [CLOUD-DEPLOYMENT-GUIDE.md](CLOUD-DEPLOYMENT-GUIDE.md) |

## ⚙️ Конфигурация

### Обязательные переменные окружения:
```bash
API_ID=ваш_telegram_api_id
API_HASH=ваш_telegram_api_hash
BOT_TOKEN=токен_бота_от_botfather
OWNER_ID=ваш_telegram_user_id
MONGO_DB_URI=mongodb://... (создается автоматически)
```

### Опциональные улучшения:
```bash
# Для webhook (более надежно)
WEBHOOK_URL=https://your-app.domain
WEBHOOK_PORT=8080

# Для логирования
LOGGER_ID=ваш_лог_канал_id
LOG_GROUP_ID=ваш_лог_группа_id
```

## 🔧 Управление

### Проверка состояния:
```bash
python3 check-deployment.py
```

### Просмотр логов:
```bash
# Railway/Render: Dashboard
# Docker: docker-compose logs -f armedmusic
# VPS: journalctl -u docker -f
```

### Обновление бота:
```bash
git pull
docker-compose down
docker-compose up -d --build
```

## 🛡️ Безопасность

- ✅ SSH ключи вместо паролей
- ✅ Firewall (UFW)
- ✅ Регулярные обновления
- ✅ Мониторинг логов
- ❌ Не коммитить `.env` в git

## 📊 Мониторинг

- **Railway/Render:** Встроенный dashboard
- **VPS:** `htop`, `docker stats`
- **Здоровье:** `python3 check-deployment.py`

## 💰 Стоимость

| Платформа | Бесплатно | $10/месяц | Особенности |
|-----------|-----------|-----------|-------------|
| Railway | 512MB RAM | 1GB RAM | Авто-развертывание |
| Render | 750h/месяц | 1GB RAM | Docker native |
| DigitalOcean | - | 1GB VPS | Полный контроль |

## 🚨 Устранение неполадок

### Бот не отвечает:
1. Проверьте логи: `docker-compose logs armedmusic`
2. Проверьте переменные: `python3 check-deployment.py`
3. Проверьте Telegram API

### Высокое потребление ресурсов:
1. Оптимизируйте код
2. Увеличьте лимиты сервера
3. Настройте кэширование

### Сетевые проблемы:
1. Проверьте firewall: `sudo ufw status`
2. Проверьте DNS
3. Проверьте порты

## 🎯 Рекомендации

### Для начинающих:
- **Railway** - самый быстрый старт
- **Render** - хорошая альтернатива

### Для опытных:
- **DigitalOcean** - полный контроль
- **AWS EC2** - для масштаба

### Для тестирования:
- Railway (бесплатно)
- Локальный Docker

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи развертывания
2. Запустите `python3 check-deployment.py`
3. Создайте issue в репозитории

**Ваш бот теперь работает 24/7 в облаке! 🎉**
