# Развертывание ArmedMusic на Windows 🪟

Пошаговое руководство для пользователей Windows.

## 📋 Требования

### 1. Python
```cmd
# Проверить установлен ли Python
python --version

# Если не установлен:
# Скачайте с https://python.org
# Установите и отметьте "Add Python to PATH"
```

### 2. Git
```cmd
# Проверить установлен ли Git
git --version

# Если не установлен:
# Скачайте с https://git-scm.com/downloads
```

## 🚀 Быстрое развертывание

### Шаг 1: Подготовка проекта
1. Откройте папку с проектом ArmedMusic
2. Скопируйте `env.example` в `.env`:
   ```cmd
   copy env.example .env
   ```

3. Откройте `.env` в блокноте и заполните:
   ```
   API_ID=ваш_api_id
   API_HASH=ваш_api_hash
   BOT_TOKEN=ваш_bot_token
   OWNER_ID=ваш_telegram_id
   ```

### Шаг 2: Запуск развертывания
Дважды кликните на файл `deploy-windows.bat` в папке проекта.

Или в командной строке:
```cmd
deploy-windows.bat
```

### Шаг 3: Настройка Railway
1. Перейдите на https://railway.app
2. Зарегистрируйтесь через GitHub
3. Создайте новый проект
4. Выберите "Deploy from GitHub repo"
5. Подключите ваш репозиторий
6. Добавьте переменные из `.env` файла

## 📊 Проверка работы

### В командной строке:
```cmd
# Проверить конфигурацию
python check-deployment.py

# Проверить статус бота
python check-bot-status.py
```

## 🛠️ Ручная настройка (если скрипт не сработал)

### 1. Инициализация Git
```cmd
git init
git add .
git commit -m "Deploy to Railway"
```

### 2. Создание GitHub репозитория
1. Перейдите на https://github.com/new
2. Создайте новый репозиторий
3. Добавьте remote:
   ```cmd
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main
   ```

### 3. Развертывание на Railway
Следуйте инструкциям в Railway Dashboard.

## 🚨 Устранение проблем

### "Python не найден"
- Переустановите Python
- Убедитесь что отметили "Add to PATH"

### "Git не найден"
- Установите Git с https://git-scm.com

### Скрипт не запускается
- Запустите командную строку от имени администратора
- Или используйте PowerShell:
  ```powershell
  Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
  .\deploy-to-railway.ps1
  ```

## 📞 Получение API данных

### API_ID и API_HASH:
1. https://my.telegram.org
2. "Create application"
3. Скопировать API ID и Hash

### BOT_TOKEN:
1. Написать @BotFather
2. `/newbot`
3. Скопировать токен

### OWNER_ID:
1. Написать @userinfobot
2. Скопировать ваш ID

## ✅ Готово!

После выполнения всех шагов ваш бот будет работать в облаке 24/7 бесплатно! 🎉

## 📱 Тестирование

1. Напишите боту `/start` в Telegram
2. Попробуйте `/ping`
3. Проверьте `/help`

Если что-то не работает, проверьте логи в Railway Dashboard.
