# Развертывание ArmedMusic на Railway 🚂

Railway - это простая платформа для развертывания приложений с бесплатным тарифом.

## Шаг 1: Регистрация на Railway

1. Перейдите на https://railway.app
2. Зарегистрируйтесь с помощью GitHub аккаунта
3. Подтвердите email

## Шаг 2: Подготовка проекта

1. **Создайте .env файл:**
   ```bash
   cp env.example .env
   ```

2. **Заполните все необходимые переменные в .env:**
   - `API_ID` - ваш Telegram API ID
   - `API_HASH` - ваш Telegram API Hash
   - `BOT_TOKEN` - токен бота от @BotFather
   - `OWNER_ID` - ваш Telegram ID
   - `MONGO_DB_URI` - оставьте пустым (Railway создаст базу)

## Шаг 3: Создание проекта на Railway

### Вариант A: Автоматическое развертывание из GitHub

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main
   ```

2. **В Railway Dashboard:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Подключите ваш GitHub аккаунт
   - Выберите репозиторий
   - Railway автоматически обнаружит `railway.json` и `Dockerfile`

### Вариант B: Ручное развертывание

1. **В Railway Dashboard:**
   - Нажмите "New Project"
   - Выберите "Empty Project"
   - Создайте новый сервис

## Шаг 4: Настройка переменных окружения

1. **В проекте Railway перейдите в "Variables"**
2. **Добавьте все переменные из вашего .env файла:**

   Обязательные:
   ```
   API_ID=ваш_api_id
   API_HASH=ваш_api_hash
   BOT_TOKEN=ваш_bot_token
   OWNER_ID=ваш_telegram_id
   ```

   Для базы данных (Railway создаст автоматически):
   ```
   MONGO_DB_URI=${{MongoDB.MONGODB_URL}}
   ```

   Опциональные:
   ```
   LOGGER_ID=0
   LOG_GROUP_ID=0
   DURATION_LIMIT=60
   STRING_SESSION=ваш_string_session
   ```

## Шаг 5: Добавление базы данных MongoDB

1. **В Railway Dashboard нажмите "+" -> "Database"**
2. **Выберите "MongoDB"**
3. **MongoDB будет автоматически подключена к вашему приложению**

## Шаг 6: Развертывание

1. **Railway автоматически начнет сборку и развертывание**
2. **Следите за логами в разделе "Deployments"**
3. **После успешного развертывания бот будет работать 24/7**

## Шаг 7: Проверка работы

1. **Напишите вашему боту в Telegram**
2. **Используйте команду `/start`**
3. **Проверьте логи в Railway Dashboard**

## Управление ботом

### Просмотр логов:
- В Railway Dashboard -> ваш проект -> "Deployments" -> "View Logs"

### Перезапуск:
- Railway Dashboard -> ваш проект -> "Settings" -> "Restart"

### Обновление:
```bash
git add .
git commit -m "Update bot"
git push origin main
```
Railway автоматически переразвернет приложение.

## Стоимость

- **Бесплатный тариф:** 512MB RAM, 1GB диск
- **Hobby:** $5/месяц - 1GB RAM, 5GB диск
- **Pro:** $10/месяц - 2GB RAM, 10GB диск

## Устранение неполадок

### Бот не отвечает:
1. Проверьте логи на ошибки
2. Убедитесь что все переменные окружения заданы правильно
3. Проверьте подключение к MongoDB

### Ошибки сборки:
1. Проверьте что `Dockerfile` корректный
2. Убедитесь что все зависимости в `requirements.txt`

### Проблемы с памятью:
- Обновитесь на платный тариф
- Оптимизируйте код бота

## Резервное копирование

Railway автоматически делает бэкапы MongoDB базы данных. Для дополнительной безопасности:
1. Экспортируйте важные данные регулярно
2. Используйте внешнюю базу данных (MongoDB Atlas)

## Мониторинг

- Railway предоставляет базовый мониторинг
- Для продвинутого мониторинга рассмотрите интеграцию с Sentry или LogRocket

---

🎉 **Поздравляем! Ваш бот теперь работает 24/7 в облаке!**
